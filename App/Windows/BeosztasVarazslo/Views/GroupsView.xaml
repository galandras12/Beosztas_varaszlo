<UserControl x:Class="BeosztasVarazslo.Views.GroupsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel MaxWidth="900" HorizontalAlignment="Left">
        <Grid Margin="0,0,0,14">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="Munkacsoportok" Style="{StaticResource PageTitle}" Margin="0" Grid.Column="0" />
            <Button Grid.Column="1" Content="+ Új munkacsoport" Style="{StaticResource AccentButton}"
                    VerticalAlignment="Center" Click="BtnAddGroup_Click" />
        </Grid>

        <TextBlock Style="{StaticResource HintText}"
                   Text="Például: 12 órás váltásban dolgozó ápolók, 8 órás takarítók, 4 órás részmunkaidősök, hétfőtől péntekig dolgozó irodai munkatársak." />

        <StackPanel x:Name="GroupsPanel" />
    </StackPanel>
</UserControl>
